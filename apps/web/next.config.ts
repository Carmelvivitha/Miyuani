import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Cesium assets are already in public/cesium folder
  // No webpack configuration needed for Next.js 16 with Turbopack
};

export default nextConfig;



